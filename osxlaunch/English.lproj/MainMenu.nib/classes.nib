{
    IBClasses = (
        {
            CLASS = CocoaUIController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                clientParameters = NSTextField; 
                masterPing = NSTextField; 
                playersTableView = NSTableView; 
                serversTableView = NSTableView; 
                totalPlayers = NSTextField; 
                totalServers = NSTextField; 
                window = NSWindow; 
                windowPreferences = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSSegmentedControl; LANGUAGE = ObjC; SUPERCLASS = NSControl; }
    ); 
    IBVersion = 1; 
}